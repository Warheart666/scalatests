package beard.test


//@Configuration
class QuillContext {

//  @Bean
//  def getQuillContext(@Autowired dataSource: DataSource with Closeable): PostgresJdbcContext[CompositeNamingStrategy2[CamelCase.type, UpperCase.type]] = {
//    new PostgresJdbcContext(NamingStrategy(CamelCase, UpperCase), dataSource)
//  }

}
